<div>
    @if ($count > 0)
        <span class="absolute -top-2 -right-3 z-10">

            <span
                class="inline-block bg-red-500 text-white text-xs font-bold rounded-full px-2 py-0.5 shadow min-w-[22px] text-center">
                {{ $count }}
            </span>
    @endif
</div>